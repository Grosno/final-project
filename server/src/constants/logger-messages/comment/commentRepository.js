const commentRepository = {
  GET_COMMENTS_BY_POST_ID_PARAMS: '[CommentRepository.getCommentsByPostId] PARAMS id={} page={} limit={}',
  GET_COMMENTS_BY_POST_ID_SUCCESS: '[CommentRepository.getCommentsByPostId] success status={} response={}',
  GET_COMMENTS_BY_POST_ID_ERROR: '[CommentRepository.getCommentsByPostId] error status={} response={}',
}

export default commentRepository;
